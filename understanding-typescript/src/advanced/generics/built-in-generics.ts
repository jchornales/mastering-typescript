// A generic type requires 1 argument to be certain what type it will hold

const names: Array<string> = []; // a sample of generic type

// promise generic types

// const promise: Promise<string> = new Promise((resolve, reject) => {
//     setTimeout(() => {
//         resolve("This is done!");
//     }, 2000);
// });

// promise.then((data) => {
//     data.split(" ");
// });
